# Plugin Interface

Standard interfaces and conformance checks for AI Agent Template plugins.

Plugin implementations must declare their name, version, contract name, contract version, owner, timeout, and failure policy. Tool outputs must follow the standard envelope and satisfy the template tool contract output schema.

